
2.服务器端，负责写服务器日志，标记聊天记录以外的所有操作

   格式                    时间  操作  人
4:用户表
        服务端维护一个用户表，标记聊天室类型，首先完成记名式的吧，以用户名为依据，建立主键，同时每一次正常退出都将保存所有数据
                 权限 
                 名字
                 密码
        以第一次启动时作为管理员，而仅有一个


6 使用多路复用
     流程
      创建套接字
      seltect 监视
      有数据发送
      创建线程，处理数据，结束线程

