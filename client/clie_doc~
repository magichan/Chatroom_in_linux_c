服务端 流程 
          初始化套接字
          选择登陆或是注册
          登陆成功
          【 A B 是以不同的线程完成的 】
           A  ：输入命令，完成本地解析，然后上传的server端
           B  ：接受 server的发过来的数据帧，并且 根据 标签做相应的操作





规则： 
  帧规则
   1.服务器向客户端发送的帧结构  SerToCliFrame
   2.客户端向服务器发送的帧结构  CliToSerFrame
   3.数据域规则： 使用\0结尾的字符串,默认不带回车
  默认设计规则：
    第一位注册者为管理员
    注册完成后自动退出，必须重新登陆
    服务端做尽可能少的操作，最好制作判断 帧的标签 ，进行输出
文件
├── client  可执行文件
├── headers 
│   ├── client.h      软件主要流程
│   ├── debug.h       调试头文件
│   ├── frame.h       数据帧头文件
│   ├── interface.h   提供截面的头文件
│   ├── tool.h        提供一些工具的头文件
│   └── user.h        维护用户列表的头文件
├── makefile      
└── src
    ├── client.c
    ├── interface.c
    └── tool.c

